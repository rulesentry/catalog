{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.rulesentry.io/schema/v3/core/profile.schema.json",
  "title": "RuleSentry v3 Profile",
  "description": "Versioned profile definition. Profiles are first-class entities that define cross-cutting operational contexts for rules and policies (e.g., compliance frameworks like HIPAA/PCI-DSS, environments like production/staging, security postures like zero-trust, industry verticals like healthcare/finance). Enables discovery, metadata, and hierarchical organization of profiles.",
  "allOf": [
    {
      "$ref": "base.schema.json#/$defs/VersionedBase"
    },
    {
      "type": "object",
      "properties": {
        "$schema": {
          "type": "string",
          "description": "JSON Schema URI for this document (optional but recommended)."
        },
        "type": {
          "const": "profile",
          "description": "Top-level entity type discriminator. Always 'profile' for profiles."
        },
        "name": {
          "type": "string",
          "description": "Human-readable profile name.",
          "minLength": 1,
          "maxLength": 128,
          "examples": ["HIPAA", "PCI-DSS", "Production Environment", "Zero Trust"]
        },
        "caption": {
          "type": "string",
          "description": "Short display label for this profile (used in compact UI views, badges, chips).",
          "minLength": 1,
          "maxLength": 64,
          "examples": ["HIPAA", "PCI", "Prod", "ZT"]
        },
        "description": {
          "type": ["string", "null"],
          "description": "Detailed description of what this profile represents, including scope and applicability.",
          "examples": [
            "Health Insurance Portability and Accountability Act (HIPAA) compliance framework for protecting Protected Health Information (PHI)",
            "Payment Card Industry Data Security Standard (PCI-DSS) requirements for organizations handling cardholder data"
          ]
        },
        "profile_type": {
          "type": "string",
          "description": "Classification of profile purpose: compliance_framework (HIPAA, GDPR), environment (production, staging), security_posture (zero_trust, defense_in_depth), industry (healthcare, finance), custom (user-defined).",
          "enum": ["compliance_framework", "environment", "security_posture", "industry", "custom"],
          "examples": ["compliance_framework", "environment", "security_posture"]
        },
        "frameworks": {
          "type": "array",
          "description": "Associated regulatory or compliance framework identifiers and citations. For compliance_framework profiles, this defines the specific regulations (e.g., ['HIPAA 45 CFR 164', 'HITECH Act']). For other profile types, lists relevant frameworks that apply (e.g., production environment might list ['PCI-DSS', 'SOC 2']).",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "default": [],
          "examples": [
            ["HIPAA 45 CFR 164", "HITECH Act"],
            ["PCI-DSS 4.0", "PCI-DSS 3.2.1"],
            ["GDPR", "ePrivacy Directive"],
            ["SOX", "GLBA"]
          ]
        },
        "parent_profile": {
          "type": ["string", "null"],
          "description": "Optional parent profile ID for hierarchical organization. Enables profile specialization (e.g., 'hipaa-phi' parent is 'hipaa', 'pci-level-1' parent is 'pci-dss'). Child profiles inherit rules and settings from parent unless explicitly overridden.",
          "pattern": "^[a-z0-9][a-z0-9_-]*$",
          "examples": ["hipaa", "pci-dss", "gdpr", "production"]
        },
        "icon": {
          "type": ["string", "null"],
          "description": "Optional icon identifier for UI display. Can be emoji, icon library name, or custom icon ID.",
          "examples": ["🏥", "shield", "lock", "globe"]
        },
        "color": {
          "type": ["string", "null"],
          "description": "Optional color for UI display (hex RGB format).",
          "pattern": "^#[0-9A-Fa-f]{6}$",
          "examples": ["#0066CC", "#FF6B35", "#4CAF50"]
        },
        "requires_profiles": {
          "type": "array",
          "description": "Optional list of profile IDs that must also be active when this profile is enabled. For example, 'hipaa-phi' might require base 'hipaa' profile.",
          "items": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9_-]*$"
          },
          "uniqueItems": true,
          "default": [],
          "examples": [["hipaa"], ["production", "logging-enabled"]]
        },
        "conflicts_with_profiles": {
          "type": "array",
          "description": "Optional list of profile IDs that cannot be active simultaneously with this profile. For example, 'development' might conflict with 'production'.",
          "items": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9_-]*$"
          },
          "uniqueItems": true,
          "default": [],
          "examples": [["development", "staging"]]
        },
        "display_order": {
          "type": "integer",
          "description": "Optional display order for consistent sorting in UI. Lower numbers appear first. Default: 100.",
          "minimum": 0,
          "maximum": 1000,
          "default": 100,
          "examples": [10, 50, 100]
        },
        "is_system": {
          "type": "boolean",
          "description": "Whether this is a system-provided profile (read-only, cannot be deleted). System profiles are shipped with RuleSentry and cannot be modified by users.",
          "default": false
        }
      },
      "required": ["type", "name", "caption", "profile_type"]
    }
  ]
}
